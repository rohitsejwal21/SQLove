/*
Duplicate Emails
Find all emails with duplicates.
*/

CODE:

SELECT
    email
FROM
    (
    SELECT
        email,
        COUNT(*) AS n_count
    FROM employee
    GROUP BY email
    ) emails
WHERE n_count > 1
